<h1>Restaurants</h1>
<% @restaurants.each do |restaurant| %>
  <li>
    <%= link_to restaurant.name, restaurant_path(restaurant) %>
    <%= restaurant.address %>
    <%= restaurant.category %>
  </li>
<% end %>
<%= link_to "Add new restaurant", :new_restaurant %>
